# plan archive 2026-03

This file keeps the detailed archive so that `docs/plan.md` can stay thin.

## completed
- [x] `bon` CLI のテンプレート生成と出力方針を見直した
- [x] skill copy を editor ごとの配置先へ実装した
- [x] path resolution / mapping とテストを整備した
- [x] code-size checker を追加した
- [x] `bin/bon.js` を責務単位で分割した
- [x] delta 長時間稼働ルールと `review delta` の標準運用を追加した
- [x] `docs/plan.md` を slim 形式へ移行した

## notes
- summary only stays in `docs/plan.md`
- detailed archive stays here and can continue monthly
