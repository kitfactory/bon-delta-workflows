const { languageGuidance, createLeanTemplate } = require('./agent_templates');
const { createOverviewTemplate, createDocStub } = require('./doc_templates');

module.exports = {
  languageGuidance,
  createLeanTemplate,
  createOverviewTemplate,
  createDocStub
};
